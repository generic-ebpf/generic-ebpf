#!/bin/sh
plat=`uname`
sed -i.bak 's/{{platform}}/'"${plat}"'/g' Makefile

if [ $plat = "FreeBSD" -a ! -d ./sys/modules/ebpf/tommyds ]; then
  mkdir sys/modules/ebpf/tommyds
fi

git submodule init
git submodule update
